#include<iostream>
#include <vector>
using namespace std;
int main() {
	int n, m;
	cin >> n >> m;
	vector<int>v(n);
	vector<int>h(m);
	for (int i = 0; i < n; i++) {
		cin >> v[i];
	}
	for (int i = 0; i < m; i++) {
		cin >> h[i];
	}
	int i = 0;
	int j = 0;
	while (j < m) {
		if (arr)
		}
}